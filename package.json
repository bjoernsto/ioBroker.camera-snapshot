{
  "name": "iobroker.camera-snapshot",
  "version": "0.1.0",
  "description": "ioBroker adapter for periodic camera snapshots without triggering full recordings",
  "author": "Manus AI",
  "license": "MIT",
  "main": "main.js",
  "dependencies": {
    "@iobroker/adapter-core": "^3.1.6",
    "ring-client-api": "^13.1.0",
    "fluent-ffmpeg": "^2.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
